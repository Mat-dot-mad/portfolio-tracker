# Development-only dependencies. NOT installed on the Pi — the deploy step
# runs `pip install -r requirements.txt` only.
#
# Install locally with:  venv/bin/python -m pip install -r requirements-dev.txt
-r requirements.txt
pytest==9.1.1
